machine:
  pre:
    - brew install ruby ; brew link --overwrite ruby
  xcode:
    version: "7.3"

dependencies:
  override:
    - brew update
    - bin/setup

test:
  override:
    - bin/test
    - bin/archive
